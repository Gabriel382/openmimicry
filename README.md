# OpenMimicry

Milestone 1 starter bundle with:
- central config loader
- profile definitions
- add-on registry and manifests
- profile resolver
- config validation
- default runtime settings
- profile-aware install script

## Quickstart

```bash
make install PROFILE=basic
make validate PROFILE=basic
make run PROFILE=basic
```
