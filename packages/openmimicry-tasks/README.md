# openmimicry-tasks

Task delegation package for OpenMimicry. Implements `TaskRuntimeAdapter` over mcp-agent, Claude Code, local-shell, and picoclaw.

Phase 0 ships only the import-stable surface. The real adapters are delivered by **M5** — see `docs/modules/M5_tasks.md`.
