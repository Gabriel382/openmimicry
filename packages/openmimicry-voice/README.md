# openmimicry-voice

Voice adapter package for OpenMimicry. Implements `STTAdapter`, `TTSAdapter`, `SpeechController`, and `WakeController` over RealtimeSTT and RealtimeTTS.

Phase 0 ships only the import-stable surface. The real adapters are delivered by **M2** — see `docs/modules/M2_voice.md`.
