# openmimicry-llm

LLM adapter package for OpenMimicry. Implements `openmimicry.core.contracts.LLMAdapter` over LiteLLM.

Phase 0 ships only the import-stable surface (`openmimicry.llm.mocks.MockLLMAdapter` stub). The real adapter is delivered by **M1** — see `docs/modules/M1_llm.md`.
