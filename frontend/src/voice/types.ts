
export type VoiceStatus = {
  mode: string
  runtime_state: string
  live_wake_enabled: boolean
  push_to_talk_enabled: boolean
  agent_voice_enabled: boolean
  last_transcript: string
  wake_name: string
}
