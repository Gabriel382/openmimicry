# OpenMimicry v1.8.4 acceptance

- [ ] Select `octomimic`; the dashboard shows `sprite2d` as the required runtime.
- [ ] Select `octomimic_vrm`; the dashboard shows `threejs` and the model loads.
- [ ] Restart backend and desktop; the selected character/runtime remain active.
- [ ] Send five text prompts; the 3D model does not resize, drift, duplicate, or disappear.
- [ ] Change 3D X/Y/Z position, Y rotation, and scale; preview updates immediately.
- [ ] Restart; the same 3D framing and animation speed return.
- [ ] Confirm listening, thinking, speaking, happy/error, wave, and celebrate motion is smooth.
- [ ] Complete a task; the bell shows an unread badge and opens Notifications.
- [ ] Click an unread notification; it becomes read.
- [ ] With Claude subscription mode configured, `Ask Claude to ...` starts a
      `claude_code` task in the configured working directory.
- [ ] Confirm `config/user.yaml` contains the selected pack/runtime and per-pack
      transform, but no API key.

